<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="metro.plascreem">

    <!-- CARACTERÍSTICAS DE HARDWARE -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- PERMISOS REQUERIDOS -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Splascreem"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- SPLASH SCREEN COMO PUNTO DE ENTRADA -->
        <activity
            android:name=".Splashcreem"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- OTRAS ACTIVIDADES -->
        <activity android:name=".LoginActivity" />
        <activity android:name=".Administrador" />
        <activity android:name=".Enlaces" />
        <activity android:name=".Personal_Administrativo" />
        <activity android:name=".Trabajadores" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".Instructores"    />
        <activity android:name=".ChatActivity" />

        <!-- INICIO: CORRECCIÓN DEL CRASH - REGISTRO DE LA NUEVA PANTALLA -->
        <activity android:name=".PrivacyAcceptanceActivity" />
        <!-- FIN: CORRECCIÓN DEL CRASH -->

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Receptor para manejar las respuestas rápidas de las notificaciones -->
        <receiver
            android:name=".NotificationReplyReceiver"
            android:enabled="true"
            android:exported="false" />

    </application>

</manifest>
